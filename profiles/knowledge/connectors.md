# Connectors

| Plugin | Marketplace id | Why |
| --- | --- | --- |
| Notion | `notion-workspace` | Live wiki / `{notion_page}` |
| Google Drive | `google-drive` | Source files and shared docs |

If neither is connected, keep a portable markdown store on the Bot's
cloud computer until the user picks a home.

Authorize in **Settings → Plugins**. Never store tokens in the repo.
