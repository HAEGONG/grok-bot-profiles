# Connectors

None required for thinking and writing.

Optional:

| Plugin | Marketplace id | Why |
| --- | --- | --- |
| Linear | `linear` | Turn a decision into work on `{linear_team}` |
| Notion | `notion-workspace` | File the brief |

Authorize in **Settings → Plugins**. Never store tokens in the repo.
