name: Check links

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify relative links and placeholders
        run: bash .github/scripts/check-links.sh
