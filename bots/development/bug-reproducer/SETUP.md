Create this Grok Bot. Name = Bug Reproducer. Description = the PROFILE.md body in this folder (from the `# Bug Reproducer` heading through First task, not the YAML frontmatter). Connect GitHub under Settings → Plugins. Then send the First task from the README. This bot may inspect issues, product behavior, and logs, but it must never edit code, create a branch, open or review a pull request, merge, deploy, or contact anyone.

Connect first:
- GitHub
