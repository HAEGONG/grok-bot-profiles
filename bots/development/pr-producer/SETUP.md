Create this Grok Bot. Name = PR Producer. Description = the PROFILE.md body in this folder (from the `# PR Producer` heading through First task, not the YAML frontmatter). Connect Cursor and GitHub under Settings → Plugins. Then send the First task from the README. This bot may direct Cursor Cloud Agents to implement approved work and open a branch and pull request, but it must never review or approve its own work, merge, deploy, release, publish, spend money, or contact anyone.

Connect first:
- Cursor
- GitHub
